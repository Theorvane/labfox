name: gitlab_models
description: "Data models shared across LabFox: GitLab DTOs and entities."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.13
  flutter_lints: ^6.0.0
  freezed: ^3.0.0
  json_serializable: ^6.8.0
  test: ^1.25.0
